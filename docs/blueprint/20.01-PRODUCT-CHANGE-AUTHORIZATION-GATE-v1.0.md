# 20.01 · Product Change Authorization Gate v1

**Estado:** Approved  
**Versión:** 1.0  
**Fecha:** 2026-07-22  
**Dominio accountable:** D01 · product-governance  
**Autoridad:** Founder directive del 2026-07-22: el gate validará permisos explícitos por operación y ruta; no inferirá autorizaciones y bloqueará todo cambio sensible no autorizado expresamente.

## 1. Behavioral Change Statement

Lovable, Codex y cualquier colaborador dejan de convertir una mejora aparentemente razonable en una ampliación silenciosa del producto. Ante una necesidad fuera del alcance aprobado, la implementación se detiene, documenta y vuelve a Founder Review antes de cambiar producto.

## 2. Propósito y valor de negocio

Vincular cada cambio sensible de producto con una autorización previa, explícita y ejecutable. El gate reduce retrabajo, regresiones y expansión accidental del alcance, preservando la velocidad de construcción dentro de límites claros.

## 3. Principio vinculante

El gate valida permisos explícitos por operación y ruta. No adivina semánticamente si una función parece lógica, necesaria o semejante a otra ya aprobada. Si el archivo exacto y la operación exacta no están autorizados por un manifiesto `Approved`, el cambio se bloquea.

- `modify` no concede `create`, `delete` ni `rename`.
- Autorizar un componente no concede una ruta pública.
- Autorizar una ruta no concede API, migración, permiso ni contrato de datos.
- Un Blueprint aprobado no basta por sí solo: la ejecución requiere manifiesto aprobado y trazable.
- El gate valida autoridad; nunca la crea, amplía ni interpreta.

## 4. Fuente ejecutable

Los manifiestos viven en `docs/governance/product-authorizations/*.json`. Cada uno declara:

- identificador único;
- rama de implementación exacta;
- Blueprint canónico aprobado;
- evidencia literal de autorización Founder;
- estado `Draft`, `Approved`, `Consumed` o `Revoked`;
- archivos exactos y operaciones autorizadas;
- rutas públicas exactas cuando corresponda;
- feature flags y pruebas obligatorias.

Los globs y directorios completos están prohibidos como permisos. Las rutas se expresan con `/` y relativas a la raíz del repositorio.
Una autorización sólo opera en la rama exacta declarada; no puede reciclarse en otra iniciativa.

## 5. Superficie protegida v1

- `src/**`;
- `supabase/migrations/**` y `supabase/functions/**`;
- configuración de producto, rutas, build, PWA y permisos;
- contratos públicos y archivos de dependencias cuando cambien comportamiento productivo.

Los documentos, artefactos generados y scripts de gobernanza permanecen bajo Governance Automation v1. No son autorización para tocar producto.

## 6. Operaciones

El diff Git se normaliza a `create`, `modify`, `delete` o `rename`. Un rename exige permiso explícito sobre origen y destino dentro de una sola entrada. Copias se tratan como creación.

## 7. Flujo

1. Founder aprueba Blueprint y alcance.
2. Se registra un manifiesto exacto en estado `Approved`.
3. La implementación modifica exclusivamente archivos y operaciones declarados.
4. `governance:product-check --base=<SHA>` compara el diff contra los manifiestos.
5. GitHub bloquea el PR ante ausencia, ambigüedad, exceso de alcance o evidencia inválida.
6. Cualquier ampliación vuelve a Founder Review y requiere una autorización nueva o una revisión aprobada del manifiesto.

## 8. Criterios de aceptación

1. Una ruta nueva no declarada falla.
2. Una función, API o migración no declarada falla.
3. `modify` no permite `create`, `delete` o `rename`.
4. Un manifiesto `Draft`, `Consumed` o `Revoked` no autoriza cambios nuevos.
5. Un manifiesto sin Blueprint admitido y `Approved` en 06 falla.
6. Una mejora visual exacta y aprobada pasa sin conceder capacidades vecinas.
7. Los mensajes identifican archivo, operación y causa del bloqueo.
8. GitHub verifica; nunca reescribe manifiestos ni adjudica aprobación.
9. La implementación del gate no toca `src/`, Supabase, UI, rutas ni datos.

## 9. Implementación autorizada

- `docs/governance/product-authorizations/README.md`
- `scripts/governance/schemas/product-authorization.schema.json`
- `scripts/governance/lib/product-authorization.mjs`
- `scripts/governance/validate-product-authorization.mjs`
- `scripts/governance/test-product-authorization.mjs`
- `.github/workflows/governance-integrity.yml`
- `package.json`

## 10. Fuera de alcance

Implementar cambios visuales, crear rutas o funciones de producto, modificar Supabase, aprobar automáticamente Blueprints, inferir equivalencias semánticas, activar Commerce o alterar el trabajo visual todavía en revisión.
