# CV8.9.2 · Decision Ingestion, Roles & Audit — Completion Report v1.0

**Estado:** implementación y migración base completas · hardening post-review pendiente de aplicar · cierre funcional pendiente.
**Fecha:** 2026-07-20.
**Rama:** `agent/cv8-9-2-ingestion-roles-audit`.
**Depende de:** CV8.9.1 Contract & Projection v1.0.0.

## 1. Resultado

CV8.9.2 entrega el único canal gobernado para proponer, transicionar, validar y
reemplazar decisiones Action Queue. Toda mutación requiere autenticación,
resuelve roles con `has_role`, deriva el estado desde eventos y añade un nuevo
`recommendation.lifecycle` al ledger existente.

No existen tablas de decisiones, filas de estado actual, snapshots mutables ni
acciones automáticas sobre campañas, contenidos, Alux, Concierge, empresas o
destinos.

La migración fue aplicada a producción con autorización explícita del Founder y
quedó registrada como versión real `20260720234745`. El smoke transaccional
validó grants, append atómico, control de estado obsoleto y supersesión sin dejar
filas de prueba. El cierre funcional todavía exige publicar el hardening en el
PR #2 y ejecutar el recorrido end-to-end con cuentas autenticadas de los roles
operativos después de integrar o desplegar las funciones de servidor.

La revisión del PR #2 detectó dos brechas antes de integración: el orden de
estado actual priorizaba `ingested_at` sobre el reloj autoritativo `occurred_at`,
y dos correcciones concurrentes podían reemplazar la misma decisión. La
migración aditiva `20260720000300_510f3111-b1b5-4de8-865c-16c25ddca454.sql`
corrige ambas sin modificar la migración ya aplicada. La prevalidación de
producción encontró 0 eventos de decisión y 0 targets duplicados.

## 2. Entregables

- `src/lib/visitor-intel/decision-operations.ts`
  - `DECISION_INGESTION_CONTRACT_VERSION = "1.0.0"`.
  - mapping `super_admin → founder` y `admin → admin`.
  - permisos assigned-only para `concierge_lead` y `editor`.
  - filtro de cola, buckets, SLA, feedback e issues por asignación.
  - constructor puro del envelope auditable.
- `src/lib/visitor-intel/decisions.functions.ts`
  - `getDecisionQueue`.
  - `proposeDecision`.
  - `transitionDecision`.
  - `attachEvidence`.
  - `supersedeDecision`.
- `supabase/migrations/20260720000200_7b6db1f9-2f0b-4bcf-9e82-3f57f9eac901.sql`
  - índice parcial por `decision_id`.
  - RPC service-only `visitor_intel.append_decision_event`.
  - serialización por advisory lock.
  - optimistic concurrency con `expected_from_state`.
  - reloj de base de datos como timestamp autoritativo.
  - bloqueo atómico de transición contra decisión reemplazada.
- `scripts/visitor-intel-decision-operations.test.ts`
  - seis pruebas de mapping, fail-closed, asignación, filtrado y auditoría.

## 3. Modelo de autorización

| Rol real | Rol funcional | Ver | Proponer/aceptar | Avanzar trabajo | Validar/rechazar | Reemplazar |
| --- | --- | --- | --- | --- | --- | --- |
| `super_admin` | Founder | Todo | Sí | Sí | Sí | Sí |
| `admin` | Admin | Todo | Sí | Sí | Sí | Sí |
| `concierge_lead` | Concierge Lead | Sólo asignadas | No | Sólo propias | No | No |
| `editor` | Editor | Sólo asignadas | No | Sólo propias | No | No |

Los roles limitados pueden ejecutar únicamente:

- `accepted → in_progress`;
- `in_progress → implemented|blocked`;
- `blocked → in_progress`.

No pueden cambiar el acuerdo humano, reasignar owner, diferir, descartar,
validar, rechazar ni reemplazar.

## 4. Defensa en profundidad

1. `requireSupabaseAuth` protege todas las funciones públicas.
2. `has_role` se evalúa en cada llamada y falla cerrado si el lookup falla.
3. El actor y su rol se estampan desde el contexto autenticado; no vienen del
   cliente.
4. Los endpoints genéricos CV8.1 y el escritor CV8.6 rechazan
   `subtype = "decision"`; sólo las funciones CV8.9.2 pueden escribirlo.
5. La tabla `visitor_intel.events` sigue sin grants para `anon` o
   `authenticated`; únicamente `service_role` accede bajo RLS forzada.
6. La RPC vuelve a validar envelope, sujeto operacional, actor, grafo y estado.
7. Advisory locks evitan dos transiciones concurrentes sobre el mismo estado.
8. Una decisión reemplazada no puede seguir avanzando.

## 5. Persistencia

| Recurso | Cambio |
| --- | --- |
| Tablas nuevas | 0 |
| Columnas nuevas | 0 |
| Snapshots mutables | 0 |
| Índices | 1 parcial sobre decisiones |
| RPCs | 1, ejecutable sólo por `service_role` |
| Eventos | Append-only en `visitor_intel.events` |

## 6. Validación técnica

| Validación | Resultado |
| --- | --- |
| Suite completa | 160/160 pass · 362 assertions |
| Tests nuevos CV8.9.2 | 6/6 pass |
| Typecheck | limpio |
| ESLint acotado | limpio |
| Build de producción | aprobado |
| `git diff --check` | limpio |
| Aplicación de migración | aprobada · versión real `20260720234745` |
| Grants DB | aprobado · sólo `service_role` ejecuta la RPC; `anon/authenticated` sin acceso directo |
| Smoke DB transaccional | aprobado · propuesta, aceptación, stale-state y supersesión · 0 filas persistentes |
| Revisión PR #2 | 2 hallazgos corregidos localmente · orden autoritativo y supersesión única |
| Migración correctiva post-review | lista · pendiente de autorización y aplicación |
| Smoke end-to-end con cuentas reales | pendiente de integrar o desplegar las funciones de servidor |

El build mantiene advertencias heredadas de TanStack `inputValidator`,
`vite-tsconfig-paths` y el glob PWA vacío. No fueron introducidas por CV8.9.2.

## 7. Outcome Validation

1. **¿Existe una única vía de escritura?** Sí en código: los canales genéricos
   rechazan decisiones y las cuatro mutaciones usan la RPC específica.
2. **¿La autorización es humana y explicable?** Sí. La matriz de permisos es
   explícita, testeada y el actor proviene del token autenticado.
3. **¿Se preserva append-only bajo concurrencia?** Sí por diseño: el RPC toma
   locks por decisión, compara estado esperado y sólo inserta.
4. **¿Hay identidades de visitantes?** No. El sujeto del evento es
   `decision:<uuid>`; el único usuario registrado es el operador auditable.
5. **¿Está validado en producción?** La capa DB sí: migración, grants y
   transiciones atómicas quedaron verificadas. Falta el recorrido end-to-end de
   las funciones de servidor con Founder/Admin/Concierge Lead/Editor una vez
   publicada la rama.

## 8. Gate siguiente

Antes de abrir CV8.9.3 se requiere:

1. [x] autorización Founder y aplicación de la migración CV8.9.2;
2. [x] verificar grants de la RPC (`service_role` sí; `anon/authenticated` no);
3. [x] probar en DB propuesta, transición, conflicto de estado y supersesión;
4. [ ] aplicar la migración correctiva post-review y probar supersesión única;
5. [ ] publicar la corrección y probar rechazo/permiso con cuentas autenticadas reales;
6. [ ] aprobar este Completion Report.
