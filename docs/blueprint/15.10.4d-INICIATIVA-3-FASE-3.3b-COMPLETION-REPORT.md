# Iniciativa 3 · Fase 3.3b · Completion Report

**Alcance:** Eliminación controlada del código v2 tras la reescritura de
rutas en 3.3a.1. Sin tocar tablas ni RPCs.

## Cambios

### Archivos eliminados
- `src/lib/experience-builder/eb-studio.functions.ts` — server fns v2
  del Studio (list/get/upsert/save/publish/rollback/themes/variants/
  preview tokens). 0 consumidores.
- `src/lib/experience-builder/eb-public.functions.ts` — server fn v2
  pública (`ebResolvePublicPage`) + helper `applyVariantOverrides`. 0
  consumidores tras 3.3a.1.

### Guard endurecido
- `scripts/assert-no-v2-imports.sh` simplificado: quitadas las
  exclusiones de los archivos v2 (ya no existen). Ahora sólo excluye
  este propio script y `docs/**` (menciones históricas OK). Confirma
  que nadie reintroduce imports o exports de `eb-studio` / `eb-public`.

## No tocado
- **RPCs vivas**: `eb_get_published_by_slug`, `eb_resolve_public_route`,
  `eb_process_scheduled_publishes`, `has_role` — intactas.
- **Tablas**: no se eliminó ninguna (`eb_pages`, `eb_preview_tokens`,
  `eb_themes`, `eb_variants`, `eb_page_versions`, etc. siguen vacías
  esperando 3.3c).
- `CompositionRenderer`, Studio, Portal, Business, Product, Surface Kit,
  Surface Composer, `page-kind-registry`.
- Ruta v1 `/preview/composition/$token` (usa `CompositionPreviewPayload`
  de `studio.functions.ts` v1, símbolo distinto — sin colisión).

## Verificación

| Chequeo                              | Resultado          |
|--------------------------------------|--------------------|
| Guard estricto post-eliminación      | ✅                 |
| `tsgo --noEmit`                      | ✅ limpio          |
| Kit smoke                            | ✅ 11/11           |
| Composer smoke                       | ✅ 8/8             |
| Business regression                  | ✅ 4/4 idénticos   |
| Product regression                   | ✅ 5/5 idénticos   |
| `/l/$slug` sobre v1                  | ✅ (3.3a.1)        |
| `/preview/$token` retirada/noindex   | ✅ (3.3a.1)        |
| `/preview/composition/$token` v1     | ✅ intacta         |

## Rollback
Los dos archivos eliminados están recuperables desde el historial de
cambios. Restaurarlos + revertir el guard reactivaría v2 sin efectos
de datos (0 filas en tablas v2).

## Próximo paso
3.3c (DROP de las 10 tablas `eb_*` vacías tras dump de seguridad) queda
desbloqueado — requiere aprobación explícita del Founder.