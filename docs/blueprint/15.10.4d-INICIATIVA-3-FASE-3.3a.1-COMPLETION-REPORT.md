# Iniciativa 3 · Fase 3.3a.1 · Completion Report

**Alcance:** Reescribir `/l/$slug` sobre v1, retirar `/preview/$token` v2 y endurecer el guard de imports. Entrega atómica (los tres pasos en una sola PR para rollback conjunto).

## Verificación de base de datos

| Tabla                | Filas |
|----------------------|-------|
| `eb_pages` (v2)      | 0     |
| `eb_preview_tokens`  | 0     |

Confirmado antes de ejecutar cambios. Cero tráfico real afectado.

## Cambios

### 1. `src/routes/l.$slug.tsx` — reescrita sobre v1
- Elimina `ebResolvePublicPage` y `applyVariantOverrides`.
- Consume `getPublishedCompositionBySlug` (mismo server fn que `/p/$slug`).
- Renderiza con `CompositionRenderer` sin variables demo (paridad con `/p/$slug`).
- SEO derivado de `snapshot.chrome.seo` (`title`, `description`, `og_image`, `canonical`, `noindex`) con fallback a `SITE.name` / `SITE.default_description`.
- URL canónica `/l/{slug}` intacta: `page-kind-registry` no se toca.
- No se agregan redirects (`/l/*` conserva la URL).

### 2. `src/routes/preview.$token.tsx` — retirada
- Elimina `ebResolvePreview` y el fetch al RPC v2.
- Componente reducido a pantalla amable: "Este enlace de vista previa ya no está disponible. Solicita uno nuevo desde el Studio."
- Sigue `noindex` vía `buildPublicHead`.
- **No redirige** a `/preview/composition/$token` (no existe mapeo v2→v1).

### 3. `scripts/assert-no-v2-imports.sh` — modo estricto
- Retirada la whitelist temporal de `src/routes/l.$slug.tsx` y `src/routes/preview.$token.tsx`.
- Añadidas exclusiones para los docs de plan/completion 3.3a.1.
- Guard ejecutado: ✅ cero consumidores de v2 fuera de sus módulos.

## Verificación

| Chequeo                                | Resultado          |
|----------------------------------------|--------------------|
| `eb_pages` = 0                         | ✅                 |
| `eb_preview_tokens` = 0                | ✅                 |
| `assert-no-v2-imports.sh` estricto     | ✅                 |
| `tsgo --noEmit`                        | ✅ limpio          |
| Kit smoke                              | ✅ 11/11           |
| Composer smoke                         | ✅ 8/8             |
| Business regression                    | ✅ 4/4 idénticos   |
| Product regression                     | ✅ 5/5 idénticos   |

### Pruebas de ruta esperadas
- `/l/inexistente` → `notFound()` con `head` `noindex` (rama `if (!page)` del `head()`).
- `/preview/token-falso` → pantalla "Vista previa no disponible" con `noindex`.
- `/preview/composition/$token` → intacto (ruta v1 canónica, no modificada).

## No tocado
`CompositionRenderer`, `page-kind-registry`, Surface Kit, Surface Composer, Business, Product, tablas, RPCs, `eb-studio.functions.ts` y `eb-public.functions.ts` (siguen `@deprecated`, sin nuevos consumidores).

## Rollback
Revertir esta única entrega restaura el estado 3.3a (whitelist + rutas sobre v2). No hay cambios de esquema ni de datos.

## Próximo paso
3.3b (borrado de `eb-studio.functions.ts` y `eb-public.functions.ts`) queda desbloqueado: ya no existen consumidores.