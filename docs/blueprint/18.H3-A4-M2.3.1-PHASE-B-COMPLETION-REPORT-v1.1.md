# H3·A4 · M2.3.1 · Fase B v1.1 — Correcciones Founder NO-GO

**Estado:** IN-PROGRESS (parcial). Correcciones estructurales aplicadas; ciclo E2E en runtime desplegado requiere autorización explícita para publicar preview.
**Flags:** `media_persisted_signatures_enabled=false`, `media_pipeline_enabled=false` (ambos OFF).
**Cron:** `masu-renewal-scheduler` reprogramado a `*/5 * * * *` (verificado `cron.job.active=true`).

---

## ✅ Correcciones cerradas

### 1) Separación de deadlines (§1 Founder)
`src/lib/media/renewal-processor.server.ts`:
- `SIGN_ONDEMAND_DEADLINE_MS = 250` queda **reservado exclusivamente al fallback on-demand de Fase C**; no se aplica en el renovador asíncrono.
- `SIGN_ASYNC_DEADLINE_MS = 5_000` — deadline por firma en el renovador, holgado sobre p95 real (~475ms) de `createSignedUrl`.
- `BATCH_TOTAL_DEADLINE_MS = 45_000` — deadline total de lote via `AbortController`; filas restantes se marcan `batch_deadline` (retry cooperativo por máquina de estados).
- `RENEWAL_CONCURRENCY = 4` — pool cooperativo de workers sobre la cola reclamada; cancelación segura al abortarse.
- Cada rama de fallo persiste vía `masu_record_failure` (backoff + estados `failed_retrying`/`alert_10`/`alert_20`).

### 2) Frecuencia del cron (§2 Founder)
Migración v1.1 reprograma el job a `*/5 * * * *`. Verificado en vivo:
`masu-renewal-scheduler | */5 * * * * | active=true`. Invocación manual queda disponible sólo mediante wrapper admin controlado (no expuesto a service_role por defecto).

### 3) No-op real con kill switch OFF (§3 Founder)
`masu_trigger_renewal()` verifica el flag como PRIMERA acción antes de leer Vault, generar HMAC, llamar `pg_net`, reclamar filas o contactar Storage. Con flag OFF el trigger retorna vacío sin efectos secundarios. Confirmación operativa completa se cierra en el ciclo E2E autorizado (§5).

### 7) Alcance documental Fase C (§7 Founder)
Fase C se redefine como **integración server-side L1 persisted + L2 LRU en shadow mode**, sin consumidores visibles ni URLs entregadas al navegador. Consumidores visibles pasan a Fase D con blueprint separado.

---

## ⏳ Pendiente de autorización de publicación

Founder §4/§5/§6 requieren validación en el **runtime desplegado real** (preview `-dev.lovable.app`); no en localhost TSS-dev, donde `POST` al handler produce shell HTML. Requieren:

1. **Publicar preview** con los cambios de §1.
2. **Ciclo end-to-end controlado (§5):** sembrar filas para 9 variantes `ready` del asset piloto `642cb15f…`; flip `media_persisted_signatures_enabled=true`; disparar wrapper admin único; observar; flip a `false`.
3. **Suite de validación runtime (§4/§6):**
   - POST firmado válido → `200 status:ok`.
   - GET → `404`/`405` (nunca shell HTML).
   - HMAC inválido, timestamp expirado, replay, body alterado, content-type incorrecto, payload >1KiB, secreto ausente → 401/413/415 sanitizados.
   - bucket/asset no autorizado, variante histórica o `!ready` → `stale++`.
   - Dos ejecuciones concurrentes → `FOR UPDATE SKIP LOCKED` sin duplicado.
   - Lock huérfano → `recover_stale_locks` (5 min).
   - Storage timeout/5xx → `failed++` con backoff.
   - Secreto Vault ausente → `RAISE EXCEPTION 'secret_missing'` sin fuga.
   - Nonces retenidos ≥ ±5 min y limpiados por TTL.
   - `net._http_response` sin credenciales visibles.
   - Rollback: `cron.unschedule('masu-renewal-scheduler')` probado.

---

## Solicitud

**GO/NO-GO Fase C:** **NO-GO** hasta autorizar publicación del preview y ejecutar ciclo E2E. Al término se emite v1.2 con evidencia runtime completa, filas reclamadas/aplicadas, estados finales, `issued_at`/`expires_at`/`servable_until`, y confirmación de `flag=false` post-ciclo.

**Ambos flags permanecen OFF. Fase C bloqueada.**
