# CV5 · Validation Report v1.0 (Parte Estática)

**Fecha:** 2026-07-13  
**Ámbito:** `/cuenta/mi-viaje` — Workspace del Viajero (7 vistas)  
**Blueprint origen:** `16.CV5-INTELLIGENT-TRAVEL-WORKSPACE-v1.0.md`  
**Estado global:** 🟡 CONDITIONAL GO (estático). Dinámico pendiente de sesión.

---

## 1. Semáforo por bloque

| Bloque | Área | Estado | Nota |
|---|---|---|---|
| 1 | Consistencia de las 7 vistas | 🟢 | Todas las vistas mapeadas en `VISTA_META` y `PHASE_VISTA_ORDER`; render único desde `mi-viaje.tsx`. |
| 2 | Flujo completo (search params, deep-links, tour) | 🟢 | `vista` y `focus` tipados en Route; `scrollIntoView` + ring temporal presente; tour + botón "?" activos. |
| 3 | Integraciones (Travel Plan · Alux · Concierge · Timeline · Maps) | 🟡 | Contrato canónico intacto; ver hallazgo H-3.1 (accesos directos a `travel_plans` fuera de barrel). |
| 4 | Fase del viaje (Founder Travel Companion) | 🟡 | Fase se deriva, no se persiste; ver hallazgo H-4.1 (nombre del helper). |
| Extra | Typecheck | 🟡 | `tsgo --noEmit` reporta errores **pre-existentes fuera de CV5** (Marketplace `$slug` y `DiscoveryNavigatorBlock`). Ver H-X.1. |
| Extra | Guardias | 🟢 | `scripts/assert-no-v2-imports.sh` verde. |

---

## 2. Hallazgos estáticos

### H-3.1 · Accesos directos a `travel_plans` / `travel_plan_items` (severidad: BAJA)
Los accesos `.from("travel_plans" | "travel_plan_items")` están **concentrados** en los módulos contrato:
- `src/lib/traveler/travel-plans.functions.ts` (owner del Write/Read Contract)
- `src/lib/traveler/travel-plan-optimize.functions.ts` (Alux optimize — permitido, escribe con confirmación de viajero)
- `src/lib/traveler/alux-traveler.functions.ts` (Read para contexto)
- `src/lib/alux/traveler-lens.functions.ts` (Read para métricas)
- `src/lib/concierge/orders.functions.ts` (Read snapshot al promover)

No hay accesos desde `components/` ni desde `routes/`. Cumple **Travel Plan Contract v1.0**. Recomendado: añadir guard `scripts/assert-plan-writes-only-in-contract.sh` en futuras olas para blindar la invariante.

**Acción:** Ninguna (informativo). Backlog Carril B.

### H-4.1 · Helper de fase nombrado `deriveCompanionPhase` (severidad: BAJA · nominal)
La memoria `mem://policies/founder-travel-companion.md` menciona `deriveTripPhase(plan, confirmed)`. La implementación real es `deriveCompanionPhase(confirmed)` en `mi-viaje.tsx:98`. Semánticamente equivalente y **la fase se calcula, no se persiste** (cumple regla vinculante 2). Sin embargo:

1. Nombre difiere del contrato documental.
2. No recibe `plan` — deriva sólo de `confirmed`, lo que impide diferenciar "planning" con plan vacío vs plan poblado sin orden.
3. Está **encapsulado dentro** de la ruta; no se puede reutilizar por Alux, Concierge o Discovery.

**Acción propuesta (Carril A · pre-CV6):** extraer a `src/lib/traveler/trip-phase.ts` como `deriveTripPhase(plan, confirmed)`, exportar `TripPhase` y consumir desde workspace/Alux/Concierge. **No bloquea CV6** si se documenta como excepción.

### H-X.1 · Errores TS pre-existentes fuera de CV5 (severidad: MEDIA · no bloqueante)
`tsgo --noEmit` reporta errores en:
- `src/components/experience-builder/blocks/DiscoveryNavigatorBlock.tsx:96` — search param `explora` no tipado en `__root`.
- `src/components/surfaces/MarketplaceSurface.tsx:63`, `product-blocks.legacy.tsx:256`, `cuenta/favoritos.tsx:98` — rutas `/marketplace/$slug` vs `/marketplace/$` (splat).

**Ninguno toca CV5 ni el workspace.** Existían antes del inicio de la validación. Backlog Carril B.

---

## 3. Verificaciones ejecutadas

- ✅ Contratos: `getMyActivePlan`, `getMyConfirmedTravel`, `getAluxConciergeContext`, `optimizePlanDay`, `getPlanItemsGeo`, `promotePlanToCase`, `proposeAluxPlanAddition` presentes y consumidos únicamente por componentes autorizados.
- ✅ Fuente única: sólo módulos `src/lib/**` acceden a `travel_plans*`; components/routes NO.
- ✅ Guardia v2 verde (`scripts/assert-no-v2-imports.sh`).
- ✅ Search params tipados en Route (`vista`, `focus`) con whitelist de vistas y truncado de `focus`.
- ✅ Deep-links: `focus={search.focus}` propagado al `EmbeddedCaseFile` + `scrollIntoView` con ring temporal (líneas 2185–2200 y 317, 739).
- ✅ Onboarding: `WorkspaceOnboardingTour` montado; botón "?" reabre vía evento custom.
- ✅ Fase se calcula (`deriveCompanionPhase`), no se persiste. `PHASE_VISTA_ORDER` reordena las 7 vistas por fase.
- ✅ Alux consume `getPlanContextForAlux` + `optimizePlanDay` + `proposeAluxPlanAddition` — nunca escribe directo.
- ✅ Concierge consume snapshot vía `promotePlanToCase`; `orders.functions.ts` sólo lee `travel_plans` para contexto de orden.

---

## 4. Bloque dinámico (Playwright)

**Estado:** ⏸ BLOQUEADO — `LOVABLE_BROWSER_AUTH_STATUS = signed_out`.

Al recibir sesión inyectada (managed Supabase) ejecutaré el guion del Demo Pack §5 en:
- Desktop `1280×1800`
- Móvil `390×844`

Cubriendo las 7 vistas, cambios de vista con preservación de scroll/foco/plan, deep-links desde `TravelNotificationsBell`, tour de onboarding, empty states, Timeline, Mapa, Alux prompts, Concierge (lista + comparación), Documentos (voucher imprimible), Recuerdos (diario + fotos), y fases Planeando/Confirmado/En viaje/Finalizado en la medida que `VMX-DEMO01` lo permita.

Se anexarán capturas y una tabla con hallazgos dinámicos, severidad y acciones.

---

## 5. Recomendación

**🟡 CONDITIONAL GO** para abrir CV6 · Live Destination Companion, condicionada a:

1. Ejecutar Sweep dinámico Playwright (desktop + móvil) cuando la sesión demo esté disponible.
2. Aceptar formalmente H-4.1 como excepción documentada, o extraer `deriveTripPhase` a `src/lib/traveler/trip-phase.ts` antes de abrir CV6 (esfuerzo estimado < 30 min).
3. H-X.1 no bloquea (errores fuera del ámbito CV5).

Si el Founder aprueba las excepciones y el Sweep dinámico no revela regresiones bloqueantes → 🟢 **GO** para CV6.