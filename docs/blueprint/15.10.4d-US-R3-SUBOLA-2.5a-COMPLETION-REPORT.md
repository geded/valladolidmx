# Sub-ola 2.5a · Kit Primitives — Completion Report

> Estado: Entregado. Libreria base del Surface Kit disponible en
> `src/components/surfaces/kit/`. Cero consumidores todavia
> (Product y Business migran en 2.5b / 2.5c). Cero cambios visuales
> en produccion.

## Alcance ejecutado

1. Carpeta creada: `src/components/surfaces/kit/`.
2. Primitives implementados (ViewModel-only):
   - `KitShell` — delega en `PublicShell`, contrato neutro.
   - `KitHero` — eyebrow / title / subtitle / badges / slot `actions`.
   - `KitGallery` — cover + snap-x mobile, grid en `sm+`.
   - `KitRichText` — heading + body + emptyLabel.
   - `KitPriceCta` — precio + `humanize(mode)` + slot `actions`, sticky opcional.
   - `KitInfoTable` — filas `label / value`.
   - `KitReviews` — Stars interno, heading y emptyLabel configurables.
   - `KitFaq` — details/summary, heading configurable.
   - `KitPromos` — grid + soporte `gated` con label.
   - `KitCardGrid` — grid 1/2/3/4, media, precio, badges y slot `actions` por tarjeta.
   - `KitContact` — tipo + label + value + href opcional.
   - `KitLocation` — label + address_line1/2 (coords reservadas para v2).
   - `KitBadge` / `KitBadges` — tonos neutral / primary / success / warning / danger.
   - `EmptyHint` — hint editorial reversible.
   - `format.ts` — `formatPrice`, `humanize`, `clampRating`.
3. Contratos ViewModel-only en `kit/types.ts`: CrumbVM, BadgeVM, MediaVM,
   PriceVM, ShellVM, HeroVM, GalleryVM, RichTextVM, PriceCtaVM, InfoRowVM,
   InfoTableVM, ContactVM, LocationVM, ReviewVM, FaqVM, PromoVM, CardVM,
   CardGridVM.

## Aislamiento verificado

- El Kit no importa de `@/lib/marketplace/**`, `@/components/surfaces/BusinessSurface`,
  `@/components/surfaces/ProductSurface`, `@/components/marketplace/**`
  ni tipos de entidad de `@/integrations/supabase/types`.
- Unica dependencia UI compartida: `@/components/discovery` (`PublicShell`)
  desde `KitShell`, alineado con el Discovery Layer.
- Router no acoplado: `KitCardGrid` usa `<a href>` nativo para no importar
  `@tanstack/react-router` en el Kit.

## No hecho (por diseno)

- `vmx.kit.*` NO registrado en `block-library.ts` ni en `preview-registry.tsx`
  (se hara en 2.5d).
- Sin migracion de `product-blocks.tsx` ni `business-blocks.tsx`
  (se hace en 2.5b / 2.5c).
- Sin cambios en `CompositionRenderer`, `preview-registry`, `ProductSurface`
  ni `BusinessSurface`.
- Sin cambios visuales en `/producto/$slug`, `/marketplace/$slug` ni Studio.

## Proximo paso sugerido

Sub-ola 2.5b · Product shims: reemplazar el cuerpo de los 10 bloques
`vmx.product.*` por shims que leen `useProduct()` y delegan al Kit via un
mapper `productToKitVM(product)`. Diff visual esperado = 0.