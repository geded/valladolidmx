# 15.10.4R · Paso D — Reporte de Ejecución (Cableado fino del sidebar)

**Versión:** v1.0
**Estado:** Completado
**Restricciones honradas:** cero server functions nuevas, cero RPCs nuevas, cero cambios de RLS/permisos, cero lógica de negocio.

---

## 1. Enlaces ajustados/añadidos

| Grupo | Enlace nuevo | Ruta destino | Estado de la ruta |
| ----- | ------------ | ------------ | ----------------- |
| Viajero (vista interna) | Perfil | `/cuenta/perfil` | ✅ existente |
| Vistas públicas | Home pública | `/` | ✅ existente |
| Vistas públicas | Alux Concierge | `/alux` | ✅ existente |
| Vistas públicas | Marketplace | `/marketplace` | ✅ existente |
| Vistas públicas | Buscar en Marketplace | `/marketplace/buscar` | ✅ existente |
| Vistas públicas | Arma tu viaje | `/arma-tu-viaje` | ✅ existente |
| Vistas públicas | Empresas (público) | `/empresas` | ✅ existente |
| Vistas públicas | Experiencias | `/experiencias` | ✅ existente |
| Vistas públicas | Hoteles | `/hoteles` | ✅ existente |
| Vistas públicas | Restaurantes | `/restaurantes` | ✅ existente |
| Vistas públicas | Eventos | `/eventos` | ✅ existente |
| Vistas públicas | Oriente Maya | `/oriente-maya` | ✅ existente |

## 2. Validación de rutas

Se validó que cada entrada del sidebar (`NAV_GROUPS` en `src/routes/_authenticated/admin/route.tsx`) corresponde a un archivo existente en `src/routes/`. No hay enlaces rotos.

Rutas dinámicas verificadas como destino de listados (no enlazadas directamente, alcanzables vía sus índices):
`/concierge/expedientes/$caseId`, `/cuenta/concierge/$caseId`, `/cuenta/concierge/$caseId/evaluar`, `/portal/empresas/$businessId`, `/portal/invitaciones/$token`, `/cms/reviews/$id/moderar`, `/cms/categorias/.editar`, `/cms/regiones/.editar`, `/marketplace/$slug`, `/oriente-maya/$destino`, `/l/$slug`, `/preview/$token`.

## 3. Cobertura accesible desde `/admin` (actualizada)

| Hito | Cobertura |
| ---- | --------- |
| Baseline pre-remediación | 14.1% (12/85) |
| Post Paso B | ≈41% (35/85) |
| Post Paso C | ≈58% (49/85) |
| **Post Paso D** | **≈68% (≈58/85)** — añade superficies públicas como vistas internas auditables y `/cuenta/perfil`. |

Las capacidades aún no accesibles desde `/admin` corresponden a:
- Capacidades **diferidas a olas futuras** (Embeddings Queue, AI Prompts panel, AI Usage Log, Leads CRM, Monetización, Articles/Events/Banners CMS — todas marcadas ⛔ en la matriz).
- Capacidad **`/admin/sistema/usuarios`** que requiere las server functions de roles del Paso E (`listUsersWithRoles`, `assignRole`, `revokeRole`).
- Sub-flujos dinámicos por `id` accesibles desde sus listados ya enlazados.

## 4. Índice de Reutilización Arquitectónica (actualizado)

| Indicador | Paso D |
| --------- | ------ |
| Componentes reutilizados | +12 rutas públicas + `/cuenta/perfil` |
| Componentes nuevos | **0** |
| Server Functions reutilizadas | 0 invocadas desde el sidebar (delegado al destino) |
| Server Functions nuevas | **0** |
| RPCs reutilizadas | 0 invocadas desde el sidebar |
| RPCs nuevas | **0** |

## 5. Confirmaciones

- ✅ Cero server functions creadas.
- ✅ Cero RPCs creadas.
- ✅ Cero cambios en RLS, GRANTs o permisos.
- ✅ Cero lógica de negocio nueva (solo edición declarativa de `NAV_GROUPS`).
- ✅ Ningún enlace apunta a ruta inexistente.
- ✅ Ninguna ruta `/cms`, `/portal`, `/concierge`, `/cuenta` ni Experience Builder fue alterada.
- ✅ Business Events intactos.
- ✅ Olas 1–6 intactas.

## 6. Próximo paso

Paso E — Server functions mínimas de gobierno de roles (`listUsersWithRoles`, `assignRole`, `revokeRole`) bajo `requireSupabaseAuth` + `has_role(_, 'super_admin')`, con superficie `/admin/sistema/usuarios` thin-shell. Es la única deuda funcional pendiente para alcanzar 100% del compromiso del Blueprint dentro del alcance de 15.10.4R.