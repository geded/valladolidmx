# CV6.5.3 · Traffic Contributor — Completion Report v1.0

**Estado:** ✅ Implementado · 12/12 tests pasando · typecheck limpio
**Fecha:** 2026-07-13
**Ola padre:** CV6 · Live Destination Companion → CV6.5 · Context-Aware Decisions
**Guardrail:** `mem://policies/founder-destination-contributors-guardrail.md`
**Política Founder:** Experience First · Decision Center · Context Engine

---

## Reglas vinculantes cumplidas

| # | Regla | Cumplimiento |
|---|---|---|
| 1 | Única puerta cartográfica | `src/lib/maps/routes.server.ts` → gateway `connector-gateway.lovable.dev/google_maps`. Sin llamadas directas a Google. |
| 2 | Contributor puro | `contributors/traffic.ts` sólo consulta ruta y normaliza señal. No decide UI, no combina con otras señales. |
| 3 | Consentimiento | Usa `scope.geo` (device) sólo si el cliente lo entrega; degrada a `hotel` / `previous_activity` / `destination_center` según `scope.traffic.origin.precision`. |
| 4 | Modos de transporte | `TravelMode` = `DRIVE | WALK | BICYCLE | TWO_WHEELER`. `trafficAware` sólo en `DRIVE`. |
| 5 | 7 estados mínimos | `TrafficStatus`: `on_time`, `leave_soon`, `leave_now`, `delay_risk`, `likely_late`, `route_unavailable`, `traffic_unknown`. |
| 6 | Explicabilidad | Payload declara origen, destino, modo, distancia, duración base y con tráfico, ETA, `bufferMinutes`, `minutesToLeave`, `rationale`, `confidence`. |
| 7 | Correlación centralizada | `destinationTrafficDecisionContributor` en `decision-center-destination.ts` — única traducción de señal a `DecisionCard`. |
| 8 | Auto-Hide | Doble filtro: (a) evaluator marca `hidden: true` en trayectos irrelevantes; (b) DC omite tarjeta si no hay acción concreta. |

## Archivos entregados

- `src/lib/maps/routes.server.ts` — helper único de Routes API v2 (server-only).
- `src/lib/traveler/traffic-status.ts` — evaluador puro (sin red, sin DB).
- `src/lib/traveler/destination-context/types.ts` — extendido: `entities[].role`, `entities[].geo`, `scope.traffic`.
- `src/lib/traveler/destination-context/contributors/traffic.ts` — implementación real: geo del CMS (`business_locations`), llamada al helper, normalización.
- `src/lib/traveler/destination-context.functions.ts` — validador Zod extendido (`role`, `geo`, `traffic`).
- `src/lib/traveler/decision-center-destination.ts` — 7 ramas de estado → `DecisionCard`.
- `src/components/traveler/NowNextLaterSurface.tsx` — marca `role: "next"` y envía `arriveBy` del próximo ítem.
- `scripts/traffic-status.test.ts` — 12 casos exigidos (12/12 pass).

## Validación

```
bunx tsgo --noEmit                   → ok
bun test scripts/traffic-status.test.ts → 12 pass / 0 fail
```

Casos cubiertos por los tests: tráfico normal, salida recomendada, salida inmediata, retraso por tráfico, llegada tarde, ruta no disponible, geo denegada, fallback sin arriveBy, walking corto (Auto-Hide), delta relevante, timeout del proveedor, arriveBy inválido.

## Notas

- Fallback CMS: `business_locations.latitude/longitude` (primario) vía `supabaseAdmin` dentro del handler (nunca a nivel de módulo).
- El signal expira en 3 min (`TTL_MS`) para reflejar rápidamente cambios de tráfico sin saturar el gateway.
- `computeRouteInternal` reutiliza el `X-Goog-FieldMask` mínimo (`routes.distanceMeters,routes.duration,routes.staticDuration`).

## Pendientes explícitamente fuera de alcance

- Selector UI de modo de transporte (queda como capacidad futura en CV6.6).
- Traffic Matrix para múltiples destinos simultáneos (no requerido por el brief).
- Persistencia histórica de ETAs / analítica de puntualidad (fuera del guardrail de Contributor).

---

**Sub-ola CV6.5.3 cerrada. Lista para aprobación del Founder.**