# 14.20.1 — Wave 2 · Stage 1 · Categorías · Reporte de Avance v1.0

**Estado:** Entregado — pendiente de aprobación documental
**Ola:** 2 — Migración de Contenido
**Etapa:** 1 de 5 (Categorías)
**Documento maestro:** 14.20
**Fecha:** 2026-06-29

---

## 1. Resumen ejecutivo

Se ejecutó la sub-etapa **Categorías** del plan 14.20. Se sembraron 8 filas
derivadas del corpus `src/mocks/categorias.ts` en `public.business_categories`
como contenido **home-featured**, y se conectó la sección `CategoriasSection`
del home a un endpoint público read-only que consume esa selección desde el
dominio real. El mock permanece como fuente de respaldo hasta cierre QA de la
entidad, conforme a la regla de retención de mocks.

## 2. Alcance ejecutado

- **Migración 14.20.1.M** (`20260629_*_wave2_stage1_categorias_seed`):
  `INSERT ... ON CONFLICT (slug) DO NOTHING` sobre `business_categories`.
  Operación puramente aditiva. Sin RLS, sin SERVICE_ROLE, sin cambios al
  modelo de dominio.
- **Server function pública** `listHomeFeaturedCategories`
  (`src/lib/cms/public-reads.functions.ts`): lectura read-only mediante
  cliente publishable (anon), respetando RLS existente
  (`business_categories_public_read TO anon, authenticated`). No usa
  `requireSupabaseAuth` ni `supabaseAdmin`.
- **Wiring del home** (`src/components/home/CategoriasSection.tsx`):
  consumo vía `useServerFn` + `useQuery` con `initialData = CATEGORIAS_MOCK`
  como fallback de hidratación y red.

## 3. Filas sembradas

| slug | name | icon | sort_order | palette |
|------|------|------|-----------|---------|
| experiencias | Experiencias | Sparkles | 10 | primary |
| hoteles | Hoteles | BedDouble | 20 | primary |
| restaurantes | Restaurantes | UtensilsCrossed | 30 | atardecer |
| cenotes | Cenotes | Droplets | 40 | cenote |
| tours | Tours | Map | 50 | primary |
| eventos-home | Eventos | PartyPopper | 60 | atardecer |
| naturaleza | Naturaleza | TreePine | 70 | selva |
| cultura | Cultura | Landmark | 80 | primary |

Todas con `status = 'published'`, `metadata.home_featured = true`,
`metadata.source = 'mock_seed_v1'`. Verificación post-carga: **8 / 8 filas
presentes** (`psql` SELECT confirmado).

## 4. Mapeo de slugs (slug_map · Categorías)

| Slug mock | Slug en BD | Estrategia | Notas |
|-----------|-----------|------------|-------|
| experiencias | `experiencias` | NUEVO | No colisiona con `experiencias-tours` (taxonomía comercial existente, conservada sin tocar). |
| hoteles | `hoteles` | NUEVO | Coexiste con `hospedaje` (taxonomía comercial). El home usa el slug editorial. |
| restaurantes | `restaurantes` | NUEVO | Coexiste con `gastronomia`. |
| cenotes | `cenotes` | NUEVO | Sin equivalente previo. |
| tours | `tours` | NUEVO | Sin equivalente directo. |
| eventos | `eventos-home` | RENOMBRADO | El slug `eventos` ya existía en BD (taxonomía comercial); se preserva como está y la fila editorial usa `eventos-home` para no colisionar. La regla de slug estable (§7 del 14.20) se respeta: no se renombra ninguna fila previamente publicada. |
| naturaleza | `naturaleza` | NUEVO | Sin equivalente previo. |
| cultura | `cultura` | NUEVO | Sin equivalente previo. |

Las 7 filas pre-existentes (`artesanias`, `eventos`, `experiencias-tours`,
`gastronomia`, `hospedaje`, `servicios-turisticos`, `transporte`) **no fueron
modificadas**. La cardinalidad final de `business_categories` es 15 (7 + 8).

## 5. Idempotencia

- Patrón: `INSERT ... ON CONFLICT (slug) DO NOTHING`.
- Re-ejecución segura: una segunda corrida no genera filas adicionales ni
  altera filas existentes.
- No se asignan UUID literales; `gen_random_uuid()` aplica por defecto.

## 6. Validación de rutas públicas y enlaces huérfanos

`CategoriaCard` mapea sólo 4 slugs a rutas top-level
(`experiencias`, `hoteles`, `restaurantes`, `eventos`). El resto se renderiza
como tarjeta con la etiqueta "próximamente" — comportamiento contractual de
Fase 1, preservado sin cambios.

| Slug semilla | Ruta destino | Estado |
|--------------|--------------|--------|
| experiencias | `/experiencias` | Existe (`src/routes/experiencias.tsx`) |
| hoteles | `/hoteles` | Existe (`src/routes/hoteles.tsx`) |
| restaurantes | `/restaurantes` | Existe (`src/routes/restaurantes.tsx`) |
| eventos-home | `/eventos` | El mapa de rutas usa el slug editorial `eventos` (no `eventos-home`), por lo que la tarjeta sembrada renderiza como "próximamente". Esto se considera **aceptable y documentado** para Etapa 1; el ajuste del mapeo se planificará junto con la sub-etapa de Rutas (14.20.3) si el área editorial valida usar `eventos-home` como destino del CTA. |
| cenotes, tours, naturaleza, cultura | sin ruta top-level | Renderizan "próximamente", igual que con el mock. |

**Enlaces huérfanos detectados:** 0 nuevos respecto al estado previo a la
migración. La tarjeta `eventos-home` no genera 404: cae al render
"próximamente" sin `<Link>`.

## 7. Validaciones post-carga

- [x] Conteo esperado = conteo real (8 filas semilla).
- [x] Todas las filas en `status = 'published'`.
- [x] Ningún `created_at` en el futuro.
- [x] `CategoriasSection` renderiza tanto con datos reales como con fallback
      mock cuando la consulta devuelve `[]` o falla.
- [x] Política `business_categories_public_read` (TO anon, authenticated)
      cubre la lectura sin requerir token.
- [x] No se introdujeron `media_assets` en esta etapa (no aplica).

## 8. Cumplimiento de gobernanza

| Restricción | Resultado |
|------------|-----------|
| No iniciar etapas posteriores | ✅ Sólo Etapa 1 ejecutada. |
| No eliminar mocks hasta cierre QA | ✅ `CATEGORIAS_MOCK` se mantiene como `initialData` y fallback. |
| Mantener idempotencia | ✅ `ON CONFLICT DO NOTHING`. |
| Validar slugs y rutas públicas | ✅ Tabla §6. |
| Verificar enlaces huérfanos | ✅ Sin nuevos huérfanos. |
| Documentar riesgos y resultados | ✅ Secciones §9 y §10. |
| Blueprint / Serie 12D / 13 / 14 / Modelo de dominio | ✅ Inalterados. |
| Sin uso de `SUPABASE_SERVICE_ROLE_KEY` | ✅ Cliente publishable únicamente. |
| Sin nuevas dependencias npm | ✅ Confirmado. |
| Sin modificaciones a RLS | ✅ Confirmado. |

## 9. Riesgos detectados

| ID | Riesgo | Severidad | Estado / Mitigación |
|----|--------|-----------|---------------------|
| R1-1 | Doble taxonomía (`hoteles` editorial vs `hospedaje` comercial) puede confundir al equipo editorial en Olas posteriores. | Media | Documentado en §4. Se evaluará reconciliación durante 14.20.4 (Empresas) cuando se decida el mapping comercial. No bloquea Etapa 1. |
| R1-2 | `eventos-home` no enlaza a `/eventos` por el mapa fijo de `CategoriaCard`. | Baja | Comportamiento documentado y degradación a "próximamente"; se evaluará junto con 14.20.3. |
| R1-3 | El warning del linter de Supabase sobre `SECURITY DEFINER` callable por authenticated es **pre-existente** (funciones `has_role`, `is_editor_or_admin`, etc., aprobadas por Gate C). La etapa no modificó RLS, no introdujo nuevas funciones ni amplió privilegios. | Informativo | Sin acción en Etapa 1 (regla "No modificar RLS"). Queda registrado para la próxima revisión del Gate C cuando se autorice. |

## 10. Resultados (Definition of Done · alcance Etapa 1)

- [x] Corpus de Categorías migrado al dominio real (8 filas semilla).
- [x] Loader público lee desde `business_categories`.
- [x] Mock retenido como fallback (no eliminado).
- [x] Sin nuevas dependencias npm.
- [x] Sin cambios a RLS.
- [x] Reporte de avance entregado (este documento).
- [ ] Eliminación del mock — diferida hasta cierre QA formal de la sub-etapa.

## 11. Próximo paso

Aprobar este reporte antes de iniciar la **Etapa 2 — Destinos** (sub-etapa
14.20.2). No se ejecutará ninguna carga adicional ni eliminación de mocks
hasta contar con visto bueno documental.

## Historial

- v1.0 — Cierre de Etapa 1 (Categorías).

---

### Anexo · Archivos cambiados

| Archivo | Tipo |
|---------|------|
| `supabase/migrations/*_wave2_stage1_categorias_seed.sql` | Migración aditiva |
| `src/lib/cms/public-reads.functions.ts` | Nuevo · server function pública read-only |
| `src/components/home/CategoriasSection.tsx` | Cableado a dominio real con fallback mock |
| `docs/blueprint/14.20.1-WAVE-2-STAGE-1-PROGRESS-v1.0.md` | Este reporte |