# 18.53 · OMXDS V1 · I4-D · Platform Dependency Baseline Reconciliation · Authorization Pack v1.0

**Versión:** 1.0
**Estado:** Approved
**Dominio:** D04 · content-experience
**Rama exacta:** `fix/omxds-i4-abc-governed-source-reconciliation-v1`
**Base canónica funcional y de alcance:** `a987181a51dcfb36f026e96f784c1bd158c7629c`
**Baseline técnico de plataforma:** `74938bf0` (package.json) + `6963500a` (bun.lock)
**Autorización:** `docs/governance/product-authorizations/PCA-2026-019.json`
**Flags requeridos:** `omxds_visual_v1_contracts_enabled=false`
**Rutas públicas nuevas:** ninguna

## 1. Objeto

Resolver exclusivamente la divergencia automática de plataforma
`@lovable.dev/vite-tanstack-config` `2.7.7 → 2.13.1` y su reescritura de
`bun.lock`, sin cambiar la base canónica `a987181a…`, sin tocar código funcional
del Experience Builder y sin alterar la aceptación humana G3.

## 2. Hecho técnico verificado

1. `package.json`: única diferencia de dependencias frente a la base es la
   devDependency `@lovable.dev/vite-tanstack-config` `2.7.7 → 2.13.1`
   (commit `74938bf0`, emitido por la plataforma).
2. `bun.lock`: el bump reescribe la entrada del paquete y elimina dos entradas
   transitivas absorbidas por él, `@lovable.dev/vite-plugin-dev-server-bridge`
   y `@lovable.dev/vite-plugin-hmr-gate` (commit `6963500a`, sólo lockfile).
3. `dependencies` de ejecución: idénticas a la base. No existe ninguna otra
   divergencia de dependencias.

## 3. Requisitos vinculantes

1. La base canónica funcional y de alcance sigue siendo `a987181a…`.
2. Los validadores afectados aceptan **exactamente** el cambio autorizado y
   siguen comparando estrictamente el resto contra su base canónica.
3. Prohibidos `skip`, comodines y allowlists abiertas. Cualquier paquete,
   versión o línea de lockfile adicional debe fallar en cerrado.
4. Sin migraciones, RPC, readers, rutas públicas, flags activos, producción ni
   despliegue.

## 4. Mecanismo canónico

Se introduce un único módulo compartido de excepción gobernada:

`scripts/omxds/lib/platform-dependency-baseline.mjs`

- `assertGovernedDependencyBaseline()` compara `dependencies` estrictamente y
  admite en `devDependencies` una sola sustitución literal
  `@lovable.dev/vite-tanstack-config` `2.7.7 → 2.13.1`.
- `assertGovernedLockBaseline()` inspecciona el diff real de `bun.lock` línea a
  línea y sólo admite líneas que pertenezcan a los tres paquetes de plataforma
  declarados, con dirección de versión exacta; cualquier otra línea falla.

## 5. Validadores modificados (paquete exacto)

1. `scripts/omxds/lib/platform-dependency-baseline.mjs` (nuevo)
2. `scripts/omxds/i3/shared-surface.evidence.mjs`
3. `scripts/omxds/i3/destination-surface.evidence.mjs`
4. `scripts/omxds/i3/business-vertical-surfaces.evidence.mjs`
5. `scripts/omxds/i3/product-experience-event-surfaces.evidence.mjs`
6. `scripts/omxds/i3/business-premium-surface.evidence.mjs`
7. `scripts/omxds/i4/editorial-builder-authoring.evidence.mjs` (I4-A)
8. `scripts/omxds/i4/editorial-builder-workflow.evidence.mjs` (I4-B)

Rutas sensibles de producto declaradas ejecutablemente en `PCA-2026-019`:
`package.json` y `bun.lock`. `PCA-2026-018` queda `Consumed` y superseded por
este paquete para esas dos rutas, evitando autorización ambigua.

## 6. Verificación

`bun install --frozen-lockfile`, `lint`, `typecheck`, `build`,
`validate:i3:0|a|b|c|d`, `validate:i4:a|b|c|r`, `governance:check`,
`governance:product-check`, `validate-full-suite --base=a987181a…`,
`sync-governance --check`.

`validate:i4:d` permanece BLOCKED exclusivamente por la aceptación humana G3
pendiente. Este paquete no modifica el Acceptance Report ni los estados
`G3-H01…H12`.
