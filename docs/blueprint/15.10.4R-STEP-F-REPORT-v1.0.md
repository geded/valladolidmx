# 15.10.4R · Paso F — Verificación final

**Versión:** 1.0
**Fecha:** 2026-06-30
**Alcance:** verificación pura. Cero código nuevo, cero RPCs, cero RLS,
cero Business Events.

## 1. Verificación de la Matriz v1.3

Se recorrió fila por fila la Matriz contra el árbol real de código:

- `src/routes/_authenticated/admin/` contiene exactamente las superficies
  autorizadas: `index.tsx`, `empresas.tsx`, `turistas.tsx`, `concierge.tsx`,
  `operaciones.tsx`, `ia.tsx`, `sistema.tsx`, `sistema.usuarios.tsx`,
  `route.tsx`. No hay rutas espurias ni faltantes.
- `src/components/admin/AdminHub.tsx` único componente nuevo del Paso C,
  reutilizado por las seis thin-shells.
- `src/lib/admin/users.functions.ts` expone únicamente
  `isSuperAdmin`, `listUsersWithRoles`, `assignRole`, `revokeRole`, todas
  con `requireSupabaseAuth` + `has_role(_, 'super_admin')`.
- Las tres RPCs `admin_list_users_with_roles`, `admin_assign_role`,
  `admin_revoke_role` (Paso E) siguen presentes y son las únicas
  superficies de escritura sobre `public.user_roles`.

Resultado: **Matriz v1.3 = código real**. Se promueve a v1.4 sin
reclasificaciones de filas.

## 2. Cobertura accesible desde `/admin`

Sidebar (`src/routes/_authenticated/admin/route.tsx`) — 8 grupos, 100% de
enlaces a rutas existentes:

- **Vista general** → `/admin`, `/admin/empresas`, `/admin/turistas`,
  `/admin/concierge`, `/admin/operaciones`, `/admin/ia`, `/admin/sistema`,
  `/admin/sistema/usuarios`.
- **Operación** → `/cms/pagos`, `/cms/observabilidad`, `/cms/alertas`,
  `/cms/actividad`, `/portal/pagos`, `/portal/actividad`.
- **Contenido** → `/cms/experience-builder`,
  `/cms/experience-builder/pages`, `/cms/empresas`, `/cms/productos`,
  `/cms/destinos`, `/cms/zonas`, `/cms/regiones`, `/cms/categorias`,
  `/cms/media`.
- **Comunidad** → `/cms/reviews`, `/concierge`, `/portal/concierge`.
- **Empresas & Portal** → `/portal/empresas`, `/portal`, `/portal/ficha`,
  `/portal/catalogo`, `/portal/galeria`, `/portal/presencia`,
  `/portal/propiedad`, `/portal/invitaciones`.
- **Viajero (vista interna)** → `/cuenta`, `/cuenta/perfil`,
  `/cuenta/favoritos`, `/cuenta/carrito`, `/cuenta/historial`,
  `/cuenta/concierge`, `/cuenta/notificaciones`, `/cuenta/actividad`.
- **Áreas** → `/cms`, `/portal`, `/concierge`.
- **Vistas públicas** → `/`, `/alux`, `/marketplace`, `/marketplace/buscar`,
  `/arma-tu-viaje`, `/empresas`, `/experiencias`, `/hoteles`,
  `/restaurantes`, `/eventos`, `/oriente-maya`.

Cobertura accesible desde `/admin`: **≈69%** (sin cambios respecto a v1.3;
Paso F no añade rutas).

## 3. Identidad del Fundador

Consulta directa a `public.user_roles` para
`geded@valladolid.com.mx`:

- Roles vigentes: **`super_admin`**, **`admin`** (más un `traveler`
  heredado del onboarding estándar; no afecta privilegios).
- Acceso confirmado a:
  - `/admin` y las seis thin-shells.
  - `/admin/sistema` y `/admin/sistema/usuarios` (gate cliente
    `super_admin` + autorización dura server-side `has_role(_, 'super_admin')`).
  - Todas las superficies de CMS, Portal, Concierge, Cuenta, Experience
    Builder, Pagos, UNC, Observabilidad y Marketplace listadas en la
    Matriz.

No se ejecuta seed, migración ni script que cree otro `super_admin`. La
identidad Fundador permanece como cuenta única durante la Serie 15.10.4R.

## 4. Presencia de `/admin/sistema/usuarios`

- **Sidebar:** grupo *Vista general*, entrada `Usuarios y roles` con hint
  `super_admin` (línea 42 de `admin/route.tsx`).
- **AdminHub dentro de `/admin/sistema`:** card `Usuarios y roles` que
  enlaza a `/admin/sistema/usuarios` con descripción
  "Listar usuarios, asignar y revocar roles (super_admin)".
- Verificación gráfica de la ruta: `createFileRoute("/_authenticated/admin/sistema/usuarios")`
  presente en `routeTree.gen.ts`.

## 5. Regresiones (chequeo no destructivo)

Se comparan los conjuntos de archivos y exports antes/después de B–E:

| Dominio | Server fns tocadas en B–E | Estado |
|---|---|---|
| CMS (`cms/*.functions.ts`) | 0 | sin regresión |
| Portal (`portal/*.functions.ts`) | 0 | sin regresión |
| Concierge (`concierge/*.functions.ts`) | 0 | sin regresión |
| Cuenta / Traveler (`traveler/*.functions.ts`) | 0 | sin regresión |
| Experience Builder (`eb-*.functions.ts`) | 0 | sin regresión |
| Pagos (`payments/*.functions.ts`, `payments/*.server.ts`) | 0 | sin regresión |
| UNC / Notificaciones (`notifications/*.functions.ts`) | 0 | sin regresión |
| Observabilidad (`observability/*.functions.ts`) | 0 | sin regresión |
| Marketplace (`marketplace/*.functions.ts`) | 0 | sin regresión |
| Business Events | 0 (intocable) | sin regresión |

Único delta total de B–E: `+ src/lib/admin/users.functions.ts`
(4 server fns) y `+ admin_*` RPCs. Todo el resto es composición de UI.

## 6. Índice de Reutilización Arquitectónica (acumulado B–E)

| Métrica | Conteo |
|---|---|
| Componentes reutilizados | **todos los dominios** (CMS, Portal, Concierge, Cuenta, EB, Pagos, UNC, Marketplace) |
| Componentes nuevos | **1** (`AdminHub`) |
| Server functions reutilizadas | **100% de las preexistentes** |
| Server functions nuevas | **4** (`isSuperAdmin`, `listUsersWithRoles`, `assignRole`, `revokeRole`) |
| RPCs reutilizadas | `has_role`, `founder_dashboard_kpis`, `transition_content_status`, etc. |
| RPCs nuevas | **3** (`admin_list_users_with_roles`, `admin_assign_role`, `admin_revoke_role`) |
| Rutas nuevas | **7** (6 thin-shells + `/admin/sistema/usuarios`) |
| RLS modificadas | **0** |
| Business Events tocados | **0** |

La remediación 15.10.4R **fortalece** el árbol existente sin duplicar
capacidades funcionales.

## 7. Cierre

Paso F completo. La Matriz se promueve a **v1.4 verificada**. Listo para
evaluar siguiente paso o cierre formal de la Serie 15.10.4R.