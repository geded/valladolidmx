# Sub-ola 2.6b · Semillas del Kit en "Nueva página" — Completion Report

**Estado:** ✅ Cerrada.
**Fecha:** 2026-07-03.
**Ámbito:** Cableado fino de UI para exponer las semillas oficiales del
Surface Kit (Event, Experience, Hotel, Restaurant, Destination, Region)
desde el diálogo "Nueva página" del Studio, usando `getKitSeed(kind).build()`
y persistiendo con el flujo existente.

## Alcance cumplido
- Diálogo "Nueva página" muestra un badge **Kit** en los kinds con
  semilla oficial (Event, Experience, Hotel, Restaurant, Destination,
  Region).
- Tras `createComposition` se llama a `saveCompositionDraft` con
  `getKitSeed(kind).build()` cuando existe semilla; el resto de kinds
  conserva el comportamiento previo (draft vacío).
- El fallo del seed inicial no bloquea la creación (log + toast
  "semilla vacía").
- El árbol persistido es idéntico al que genera `SurfaceComposer`
  (mismo camino que 2.6), por lo que `CompositionRenderer` lo
  interpreta sin ajustes.

## No tocado
- `CompositionRenderer`, `preview-registry`, `block-library`.
- Business (`vmx.business.*`) y Product (`vmx.product.*`) — seeds y
  renderers intactos.
- Primitives del Surface Kit — intactos.
- Rutas públicas existentes — intactas.

## Verificaciones
```
bunx tsgo --noEmit                              → 0 errores
bun run scripts/kit-blocks-smoke.tsx            → 11/11 OK
bun run scripts/surface-composer-smoke.tsx      → 8/8 OK
bun run scripts/business-shim-regression.tsx    → 4/4 idénticos
bun run scripts/product-shim-regression.tsx     → 5/5 idénticos
```

## Archivos
- **Modificados:** `src/components/experience-builder/PagesPanel.tsx`.
- **Nuevos:** `docs/blueprint/15.10.4d-US-R3-SUBOLA-2.6b-COMPLETION-REPORT.md`.

## Flujo demo
1. Studio → Pages → **Nueva página**.
2. Elegir "Evento" (aparece badge **Kit**).
3. Nombre + slug → Crear.
4. La página abre con la semilla Kit (Hero, Badges, Gallery, RichText,
   InfoTable, Location, Contact, Faq), editable como cualquier otra
   composición.
5. Business/Product siguen creándose y renderizándose como antes.

> Cuchillos afilados, primeros pacientes en la mesa.
