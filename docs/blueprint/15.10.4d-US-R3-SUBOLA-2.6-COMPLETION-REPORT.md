# Sub-ola 2.6 · Surface Composer — Completion Report

> Estado: Entregado. Motor declarativo para componer superficies nuevas
> con bloques `vmx.kit.*`, mas semillas oficiales para Event, Experience,
> Hotel, Restaurant, Destination y Region. Cero cambios en Business /
> Product / `CompositionRenderer` / `preview-registry` / `block-library`.

## Alcance ejecutado

1. **Surface Composer** (`src/lib/experience-builder/surface-composer.ts`).
   API fluida e inmutable (`SurfaceComposer.create().add(...).build()`)
   que produce un `CompositionTree` canonico —mismo shape que emite el
   Studio— con dos guardas:
   - Whitelist en vivo derivada de `KIT_BLOCK_CONTRACTS`; cualquier
     tipo fuera de `vmx.kit.*` lanza (protege Business/Product).
   - `isKitOnlyTree(tree)` como pre-check para persistir semillas.
2. **Kit Seeds Registry** (`src/lib/experience-builder/kit-seeds.ts`).
   6 semillas oficiales declarativas (Event 8, Experience 8, Hotel 10,
   Restaurant 8, Destination 7, Region 6 bloques), todas 100 % `vmx.kit.*`.
   IDs deterministas para idempotencia al reimportar.
3. **Regresion** (`scripts/surface-composer-smoke.tsx`).
   8/8 chequeos OK, 0 fallos:
   - 6/6 semillas construyen, cumplen `isKitOnlyTree`, tienen renderer
     registrado en `KIT_BLOCK_RENDERERS`, generan markup no vacio y
     conservan IDs entre invocaciones.
   - 2/2 guardas: el Composer rechaza `vmx.business.*` y `vmx.product.*`.

## No hecho (por diseno)

- Sin cambios en `CompositionRenderer`, `preview-registry`,
  `block-library`, `page-kind-registry`.
- Sin migraciones SQL: el sistema de templates (`is_template`,
  `template_of_kind`, RPC `eb_mark_composition_as_template`) ya existia
  desde 15.10.4b Fase 2 y las semillas se materializan como
  composiciones normales.
- Sin tocar Business ni Product (bloques, shims, mappers, superficies).
- Sin registrar rutas publicas nuevas: Event/Hotel/etc. ya estan en
  `PAGE_KIND_REGISTRY`; publicar una composicion-semilla usa el flujo
  estandar del Studio.

## Regresion obligatoria

| Prueba                              | Resultado                          |
| ----------------------------------- | ---------------------------------- |
| `kit-blocks-smoke.tsx`              | 11/11 bloques OK                   |
| `product-shim-regression.tsx`       | 5/5 bloques identicos, 0 diffs     |
| `business-shim-regression.tsx`      | 4/4 bloques identicos, 0 diffs     |
| `surface-composer-smoke.tsx` (nuevo)| 8/8 chequeos OK                    |

## Metrica de reutilizacion

| Superficie   | Bloques | 100 % `vmx.kit.*` |
| ------------ | ------- | ----------------- |
| Event        | 8       | si                |
| Experience   | 8       | si                |
| Hotel        | 10      | si                |
| Restaurant   | 8       | si                |
| Destination  | 7       | si                |
| Region       | 6       | si                |

Cumple el objetivo Founder de "componer, no codificar" para las
superficies futuras autorizadas en 2.6.

## Proximo paso sugerido

Sub-ola 2.6b: exponer las semillas del Kit desde el dialogo "Nueva
pagina" del Studio (llamada a `getKitSeed(kind).build()` y persistir con
el flujo existente), sin tocar `CompositionRenderer` ni
`preview-registry`.
