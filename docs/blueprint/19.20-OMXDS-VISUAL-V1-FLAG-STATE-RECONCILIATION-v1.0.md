# 19.20 · OMXDS Visual V1 · Flag State Reconciliation v1.0

**Estado:** Approved
**Versión:** 1.0
**Dominio:** D02 · platform-foundation
**Autoridad:** Founder Directive 2026-08-25 · "AUTORIZO EXCLUSIVAMENTE EL PAQUETE DE RECONCILIACIÓN DEL FLAG."

> Nota de numeración: la directiva Founder nombró este paquete como "19.19". Ese número ya estaba
> ocupado por `19.19-V1-P1c-GOVERNED-SOURCE-IMAGE-LIBRARY-COMPLETION-REPORT-v1.0.md` (admitido en
> el Master Index v0.42). Para preservar la unicidad canónica del índice, este blueprint se admite
> como **19.20** sin alterar el alcance autorizado.

## 1. Objetivo único

Restablecer `public.platform_settings.omxds_visual_v1_contracts_enabled` de `true` a `false`
y acreditar objetivamente la reconciliación.

## 2. Estado y divergencia

| Concepto                             | Valor                                                                                                                    |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| Estado inicial observado             | `true` (`updated_at` 2026-08-25 06:57:54.807278+00)                                                                      |
| Estado gobernado requerido           | `false`                                                                                                                  |
| Postcondición exigida                | `false`                                                                                                                  |
| Fecha de detección de la divergencia | 2026-08-25                                                                                                               |
| Origen del valor                     | Fila persistida en `public.platform_settings`; no proviene de variables de entorno ni del default de migración (`false`) |
| Dependencia bloqueante               | V1-P1.d no puede iniciarse mientras la divergencia siga abierta                                                          |

`false` **no** es precondición de PCA-2026-022: es su postcondición verificable.

## 3. Alcance permitido

1. Este blueprint (19.20).
2. `docs/governance/product-authorizations/PCA-2026-022.json` en estado `Approved`.
3. Una migración idempotente única que actualice exclusivamente esa fila.
4. Aplicación al backend Cloud compartido.
5. Relectura directa de la fila acreditando `value=false` y `updated_at`.
6. Evidencia de reconciliación en `docs/governance/evidence/`.
7. Regeneración de índices sólo con el sincronizador canónico.
8. Ejecución de los gates autorizados.

## 4. Fuera de alcance (prohibido)

- Iniciar o implementar V1-P1.d.
- Modificar superficies, contratos, plantillas, rutas o `types.ts`.
- Cambiar cualquier otro registro de `platform_settings` o activar otro flag.
- Convertir `validate:i4:d` artificialmente en `PASS`.
- Alterar I4-D Functional Acceptance `PASS` u Operational Multi-Actor Acceptance `DEFERRED`.
- Desplegar cambios ajenos al paquete.

## 5. Sentencia canónica

```sql
UPDATE public.platform_settings
SET value = to_jsonb(false)
WHERE key = 'omxds_visual_v1_contracts_enabled'
  AND value IS DISTINCT FROM to_jsonb(false);
```

Idempotente: una segunda ejecución afecta 0 filas.

## 6. Invariantes conservados

- I4-D Functional Acceptance: `PASS`.
- Operational Multi-Actor Acceptance: `DEFERRED` (G3-H07, G3-H11, G3-H12).
- `validate:i4:d`: `FAIL-CLOSED` por diseño.
- V1-P1.d: no iniciada.
