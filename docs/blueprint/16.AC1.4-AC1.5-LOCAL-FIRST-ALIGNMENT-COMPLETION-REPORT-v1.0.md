# 16.AC1.4–AC1.5 · Local-first Alignment — Completion Report v1.0

**Fecha:** 2026-07-20

**Estado:** cierre técnico; pendiente de revisión, publicación e integración.

**Rama:** `agent/ac1-local-first-alignment`
**Autorización Founder:** confirmada el 2026-07-20.

## 1. Resultado

AC1 queda alineado bajo un único contrato operativo:

- antes del registro, favoritos, destinos e ítems viven exclusivamente en `AnonymousTravelDraft` del dispositivo;
- las interacciones anónimas no crean cuentas, filas DB, RPCs, Realtime ni llamadas de red;
- `guest-queue` deja de ser un modelo activo; sólo existe una lectura local de compatibilidad que lo convierte una vez y elimina su clave después de persistir el nuevo contrato;
- después de autenticar, `importAnonymousDraft` fusiona el contenido con el Travel Plan canónico;
- el contenido local sólo se elimina tras una respuesta exitosa de la importación;
- no se añadió migración, tabla, índice, función SQL ni secreto.

## 2. Implementación

| Área | Resultado |
| --- | --- |
| Store local | Cache compartido entre superficies, suscripción reactiva, reconciliación IDB/localStorage por `updatedAt` y debounce global de 400 ms. |
| Compatibilidad | Conversión pura desde `travel_plan_guest_queue`; el módulo paralelo anterior fue retirado. |
| Superficies | Favoritos, “Agregar a Mi Viaje”, preview público y Arma tu Viaje consumen el mismo draft. |
| Registro progresivo | Sólo guardar permanentemente, otro dispositivo, compartir, recordatorios y límite duro abren el prompt AC1. |
| Importación | Server fn autenticada, RLS, targets deduplicados, notas con UUID determinista, favoritos por upsert y traza sin PII en `travel_plans.meta`. |
| Recuperación | Error remoto conserva el draft y ofrece reintento; éxito limpia local e invalida Travel Plan/Favoritos. |

## 3. Idempotencia y privacidad

- Targets: dedupe por `(plan_id, item_kind, target_id)`.
- Notas: identificador estable derivado de `draftId + sourceKey` para que un reintento no duplique.
- Favoritos: `upsert` aditivo; nunca se usa `toggle` durante importación.
- Plan: fechas y tamaño de grupo sólo completan campos todavía vacíos.
- Traza: máximo 50 referencias de importación con `draft_id` e `imported_at`, sin contenido, PII ni identidad anónima.

## 4. Evidencia técnica

| Verificación | Resultado |
| --- | --- |
| Test focal AC1 | 7/7 aprobados; 2,023 assertions, incluido el orden importación → borrado y la conservación ante error. |
| Capacidad anónima | 1,000 sesiones × 20 interacciones = 20,000 acciones locales. |
| Red durante etapa anónima | 0 llamadas a `fetch` en el harness focal; auditoría estática sin Supabase/server fn en el store local. |
| Suite completa | 172/172 pruebas aprobadas; 2,397 assertions. |
| TypeScript | `tsc --noEmit` aprobado. |
| Lint acotado | Archivos AC1 nuevos/modificados aprobados sin errores. La deuda global heredada no se amplió ni se mezcló. |
| Build | Build de producción aprobado. Conserva warnings heredados de `inputValidator` deprecado y del glob PWA sin coincidencias. |
| Diff | `git diff --check` aprobado. |

## 5. Límites de este cierre

- No demuestra todavía el flujo autenticado contra producción; requiere preview/despliegue y smoke humano.
- AC1.6 (batcher de analítica remota) permanece fuera de alcance para preservar cero red en esta alineación.
- No se publica ni integra esta rama sin la autorización correspondiente.

## 6. Smoke requerido después de desplegar

1. Como visitante, guardar un favorito y agregar una experiencia sin iniciar sesión.
2. Recargar y confirmar continuidad en el mismo navegador.
3. Confirmar que “Guardar mi viaje” permite cerrar el prompt y seguir planeando.
4. Autenticarse y abrir Mi Viaje.
5. Confirmar que favoritos, ítems y metadatos aparecen una sola vez.
6. Recargar otra vez y confirmar que no se repite la importación.
7. Simular un error de importación en preview y confirmar que el contenido local permanece.

## 7. Recomendación

Abrir un Draft PR para revisión. Tras aprobarlo, integrar, desplegar y ejecutar el smoke del §6 antes de declarar cierre operativo de AC1.4–AC1.5.
