# AC1.2 · Surface Rewiring — Completion Report v1.0

**Fecha:** 2026-07-13  
**Iniciativa:** Anonymous Travel Continuity (AC1)  
**Sub-ola:** AC1.2 · Surface Rewiring  
**Estatus:** ✅ Implementada — pendiente aprobación Founder para cierre.

## 1. Alcance entregado

- `FavoriteButton` y `AddToTravelPlanButton` reconectados al dominio anónimo (`AnonymousTravelDraft`) cuando no hay sesión, sin bloqueo por `SignInPromptSheet`.
- Alux acompaña desde el primer clic (Anonymous Travel Continuity + Travel Companion First).
- Toasts reemplazados por microinteracciones conversacionales del Concierge (Founder Intent Recognition Principle) — idéntico comportamiento para anónimo y autenticado.
- Límites duros se resuelven con copy positivo (`intent.limitFriendly`), nunca como error.
- Cero cambios de dominio, cero red por interacción anónima, cero regresión para usuarios autenticados.

## 2. Política nueva registrada (Core)

- `mem://policies/founder-intent-recognition.md` — Founder Intent Recognition Principle (AC1.2).
- Referenciada desde `mem://index.md` (Core).

## 3. Contratos

- `AnonymousTravelDraft v1.0.0` — sin cambios (aditivo cero).
- `TravelPlan` / `addPlanItem` / `toggleFavorite` — sin cambios.
- `ANON_COPY.intent.*` — extensión aditiva del copy oficial del Concierge Voice.

## 4. Componentes tocados

- `src/lib/traveler/anonymous-draft/copy.ts` — bloque `intent` (5 mensajes conversacionales + `limitFriendly`).
- `src/components/commerce/FavoriteButton.tsx` — rama anónima + toasts conversacionales; `useProtectedAction` retirado.
- `src/components/traveler/AddToTravelPlanButton.tsx` — rama anónima + toasts conversacionales; `useProtectedAction` retirado.

## 5. Matriz de comportamiento

| Acción                                  | Anónimo                                                                 | Autenticado                                            |
| --------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------ |
| Marcar favorito                         | Escribe local; toast `favoriteAcknowledged`                             | Escribe DB; toast `favoriteAcknowledged`               |
| Quitar favorito                         | Elimina local; toast `favoriteReleased`                                 | Elimina DB; toast `favoriteReleased`                   |
| Agregar a Mi Viaje                      | Escribe local; toast `planAcknowledged`                                 | Escribe DB; toast `planAcknowledged` / `planAlready`   |
| Ya estaba en el viaje                   | Idempotente (skip); toast `planAcknowledged` (no diferencia técnica)    | Idempotente servidor; toast `planAlready`              |
| Límite alcanzado (25 fav / 40 ítems)    | Sin error técnico. Toast `intent.limitFriendly` (registro como beneficio) | N/A                                                    |

## 6. Cumplimiento de restricciones Founder

- ✅ Sin IA generativa.
- ✅ Sin inferencias permanentes sobre gustos/preferencias.
- ✅ Sin memoria paralela (dominio existente exclusivamente).
- ✅ Sin modificación de dominio ni contratos congelados.
- ✅ Prohibiciones Concierge Voice respetadas: cero mención a "borrador/draft/almacenamiento/IndexedDB/TTL/migración".
- ✅ Copy conversacional describe SÓLO acción del viajero + apoyo de Alux; no afirma conocer al viajero.

## 7. Validación

- Typecheck `tsgo --noEmit` → 0 errores.
- Cero escrituras de red en flujo anónimo (verificable en `network` panel).
- Ambos botones conservan estilo, iconografía y aria-pressed previos.

## 8. Siguiente

Pendiente autorización Founder para iniciar **AC1.3 · Delight Moment de Continuidad** (saludo del Concierge cuando el visitante anónimo regresa y aprovechamiento de `hasReturningTrip` / `acknowledgeReturn` ya expuestos por `useAnonymousTrip`).